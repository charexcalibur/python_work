2017/4/3
1.title(),以首字母大写的方式来显示每个单词
2.lower(),将字符串转换为小写
3.upper(),将字符串转话为大写
4.\t,缩进
5.\n,换行
6.rstrip(),暂时剔除字符串末尾的空白
7.lstrip(),暂时剔除字符串开头的空白
8.strip(),暂剔除字符串两端的空白
9.str(),将非字符串值表示为字符串
10.append(),向列表中添加元素
11.insert(),可以在列表的任何位置添加新元素，需要指定索引和值
12.del语句，删除列表中指定位置的值，del motocycles[0],删除后，无法再次访问
13.pop(),弹出列表末尾的元素，并能接着使用。也可弹出指定位置的元素，如：pop(0)，弹出第一的元素
14.remove(),当知道要被删除元素的值时使用，使用remove()从列表中删除元素时，也可以继续使用它的值
15.sort(),按顺序对列表进行排永久列，此方法可以接收参数。例如：sort(reverse=True),可以将列表按字母顺序相反的顺序排列
16.sorted(),对列表进行临时排序
17.reverse(),反转列表元素的排列顺序，再次调用可以恢复到原来的排列顺序
